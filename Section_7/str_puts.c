#include <stdio.h>

int main(int argc, const char *argv[])
{
  printf("%s\n", "Hello!");
  puts("Hello!"); // automatically puts newline character
  // gets - depricated
  // fgets - still usable (getting string from console)

  return 0;
}
